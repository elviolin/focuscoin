# 플래닛 일일 챌린지 — 클라이언트 통합 가이드 (v2)

부모 앱(WebView)에 Framer로 디자인된 3개의 일일 미션 챌린지를 통합하는 가이드.

> **v2 변경 요약 (2026-06-07)**
> - 물 마시기 챌린지가 **Supabase 백엔드로 완전 전환** (구 planit-study API 제거)
> - 물 마시기: 잔 수 DB 영속화 (새로고침/재접속해도 유지), 등록 간 **1분 쿨다운 서버 강제**
> - 빠른 등록 칩 +2/+3/**+4**잔으로 변경 (구 +5잔 제거, 1회 최대 4잔)
> - `env` 파라미터 더 이상 사용 안 함 (Supabase 단일 환경)
> - 영단어/영양제는 아직 백엔드 미연동 (UI 완성, mock 동작) — Supabase 확장 예정

---

## 1. 챌린지 목록

| 챌린지 | 설명 | 보상 | 백엔드 |
|---|---|---|---|
| **물 마시기** | 하루 8잔 (한 번에 +1/+2/+3/+4잔, 등록 간 1분 쿨다운) | +10 캐시 | ✅ Supabase 연동 완료 |
| **영단어 챌린지** | 5개 단어 학습 → 5문제 테스트 (3문제 이상 정답 시 통과) | +10 캐시 | ⏳ 미연동 (mock) |
| **영양제 챙기기** | 유저가 등록한 영양제 모두 체크 | +10 캐시 | ⏳ 미연동 (mock) |

세 챌린지 모두 동일한 디자인 시스템(Toss-style, 그린 메인 컬러, Pretendard 폰트)을 공유하며, 완료 흐름도 동일합니다.

---

## 2. 통합 방식

각 챌린지는 **Framer에 게시된 별도 URL**로 운영되며, 부모 앱에서 **WebView로 임베드**해 사용합니다.

### URL 형식

```
https://www.focuscoin.app/challenge_water?userId={USER_ID}
https://www.focuscoin.app/challenge_pill?userId={USER_ID}
https://www.focuscoin.app/challenge_english?userId={USER_ID}
```

### URL 파라미터

| 파라미터 | 필수 | 설명 |
|---|---|---|
| `userId` | ✅ | 사용자 식별자. **유저별 진행 상황이 이 값 기준으로 DB에 분리 저장**됨 |

> ⚠️ `userId`가 누락되면 컴포넌트는 mock 모드로 동작합니다 (Framer 캔버스 테스트용 — DB 저장 안 됨). **프로덕션 환경에서는 반드시 전달**해야 합니다.
> ⚠️ (v2) 구 버전의 `env` 파라미터는 물 마시기에서 제거됐습니다. 전달해도 무시됩니다.

---

## 3. WebView 권장 사이즈

| 챌린지 | width × height |
|---|---|
| 영단어 | **390 × 745** |
| 영양제 | **390 × 720** |
| 물 마시기 | **390 × 640** |

WebView 컨테이너는 이 사이즈 이상으로 설정. 컴포넌트는 부모 영역의 100%를 차지하도록 구현되어 있습니다.

---

## 4. 물 마시기 사용자 흐름 (Supabase 연동 v2)

```
1. WebView 로드 (?userId= 전달)
2. 컴포넌트가 Supabase에서 오늘(KST) 잔 수/완료 여부 자동 로드
   └─ 로딩 중엔 하단 버튼이 "불러오는 중..." 비활성 상태
3. 물 등록 (+1 메인 버튼 / +2 +3 +4 빠른 칩)
   ├─ 등록 성공 → 잔 수 즉시 반영 + DB 저장
   └─ 1분 내 재등록 시도 → 토스트 "다음 물 등록은 1분 뒤부터 가능해요!" (서버가 차단)
4. 8잔 달성 → "모두 마셨어요" CTA
5. 5초 광고 시청 (현재 mock, AdMob 연동 예정)
6. ── 광고와 병렬로 Supabase 보상 RPC 호출 ──
7. 응답별 팝업:
   ├─ 성공      → 🎉 "보상이 적립되었어요"
   ├─ duplicate → ✅ "이미 받은 보상이에요" (하루 1회)
   └─ 오류      → ⚠️ "일시적인 오류" + 다시 시도 / 닫기
8. "확인" 탭 → "오늘 챌린지 완료 ✓" 비활성 상태
9. 새로고침/재접속해도 상태 유지. KST 자정에 자동으로 새 하루 시작
```

### 유저별 독립 보장 (실측 검증 완료)

- `userId`가 다르면 잔 수, 쿨다운, 보상이 전부 따로 관리됨
- A 유저가 쿨다운 중이어도 B 유저는 정상 등록 가능
- 동일 `userId`로 여러 기기에서 접속해도 같은 진행 상황 공유

---

## 5. WebView ↔ 부모 앱 통신

**현재 단방향**: WebView가 부모 앱으로 신호를 보내지 않습니다. 챌린지 완료 후 사용자가 부모 앱 네비게이션으로 직접 닫아야 합니다.

### 추후 개선 가능

- **postMessage 통신**: 보상 적립 완료 시 부모 앱에 이벤트 전송 → WebView 자동 닫기 + 캐시 잔액 새로고침 (`Challenge_water.tsx`의 `handleClaim`에 `window.parent.postMessage(...)` 추가)
- **딥링크**: 챌린지 완료 후 부모 앱 특정 화면(캐시 잔액, 마이페이지 등)으로 이동

---

## 6. 광고 영역 (AdMob)

현재 모든 챌린지에서 **5초 mock 광고**를 사용합니다. 실제 AdMob 연동 시:

1. 각 챌린지 `.tsx` 파일의 광고 관련 함수에서 `setInterval`로 5초 타이머 돌리는 부분을 AdMob SDK 콜백으로 교체
2. 광고 시청 검증을 서버에서 하려면 **AdMob SSV(Server-Side Verification)** 별도 구현 필요 (현재 보상 RPC는 광고 시청을 검증하지 않음)

---

## 7. 데이터 영속성 (v2 현황)

| 챌린지 | 영속성 |
|---|---|
| **물 마시기** | ✅ **Supabase DB 저장** — 새로고침/재접속/기기 변경에도 유지, KST 자정 리셋 |
| 영단어 | ❌ 메모리만 (새로고침 시 초기화) |
| 영양제 | ❌ 메모리만 (등록 목록 사라짐) |

영단어/영양제도 물 마시기와 같은 패턴(Supabase 테이블 + RPC)으로 확장 예정. `mission_completions` 테이블은 이미 3개 미션 공용으로 설계돼 있습니다.

---

## 8. 디자인 시스템 (참고)

| 토큰 | 값 |
|---|---|
| 메인 컬러 (그린) | `#06C167` |
| 그린 배경 | `#E6F9F0` |
| 그린 다크 | `#048A4A` |
| 텍스트 | `#191F28` / `#6B7684` / `#9AA0A8` / `#C9CDD2` |
| 배경 | `#FFFFFF` |
| 카드 배경 | `#F8F9FA` |
| 디바이더 | `#F2F4F6` |
| 폰트 | Pretendard Variable |
| 카드 라운드 | 16px |
| 버튼 라운드 | 12px |
| 시트 라운드 (top) | 28px |

---

## 9. 파일 목록

| 파일 | 챌린지 | 위치 |
|---|---|---|
| `Challenge_water.tsx` | 물 마시기 (Supabase v2) | GitHub `elviolin/focuscoin` + Framer |
| `Challenge_pill.tsx` | 영양제 | Framer |
| `Challenge_english.tsx` | 영단어 | GitHub `elviolin/focuscoin` + Framer |
| `focuscoin_water_schema.sql` | 물 마시기 DB 스키마 (테이블 + RPC + 보안) | GitHub `elviolin/focuscoin` |

백엔드 상세는 `SUPABASE_BACKEND_HANDOVER.md` 참고.

---

## 체크리스트 (배포 전)

- [x] Framer에서 챌린지 게시 → URL 확보 (`www.focuscoin.app/challenge_*`)
- [x] 물 마시기 Supabase 연동 + 유저별 독립 진행 (2026-06-07 라이브 E2E 검증 완료)
- [ ] 부모 앱에서 챌린지 진입점 추가 (메뉴/홈/미션 탭 등)
- [ ] WebView 컨테이너 크기를 권장 사이즈 이상으로 설정
- [ ] URL 빌드 로직 구현 (`userId` 주입)
- [ ] 부모 앱 캐시 잔액 ↔ `mission_completions` 연동 방식 결정 (서버 조회 or postMessage)
- [ ] 광고 영역을 실제 AdMob으로 교체 (선택)
- [ ] 영단어/영양제 Supabase 확장 (선택)
